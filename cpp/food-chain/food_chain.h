#ifndef FOOD_CHAIN_H
#define FOOD_CHAIN_H

#include <string>

namespace food_chain {
    std::string const verse(size_t n);
    std::string const verses(size_t begin, size_t end);
    std::string const sing();
}

#endif // FOOD_CHAIN_H
